import FlexberryEnum from 'ember-flexberry-data/transforms/flexberry-enum';
import УдовлКлEnum from '../enums/i-i-s-proba1-удовл-кл';

export default FlexberryEnum.extend({
  enum: УдовлКлEnum,
  sourceType: 'IIS.Proba1.УдовлКл'
});
